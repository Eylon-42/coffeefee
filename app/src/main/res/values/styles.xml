<resources>
    <!-- Custom styles that are not themes go here -->
    <style name="ToolbarStyle" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/white</item>
    </style>

    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <!-- Search text color -->
        <item name="android:textColorPrimary">?android:attr/textColorPrimary</item>
        <!-- Hint text color -->
        <item name="android:textColorHint">?android:attr/textColorSecondary</item>
        <!-- Icon colors -->
        <item name="android:tint">?android:attr/textColorSecondary</item>
        <!-- Search plate background -->
        <item name="searchViewStyle">@style/Widget.AppCompat.SearchView</item>
        <item name="queryBackground">@null</item>
        <item name="submitBackground">@null</item>
        <item name="closeIcon">@drawable/ic_arrow_back</item>
        <item name="searchIcon">@drawable/ic_search</item>
    </style>

    <style name="CoffeeCardStyle">
        <item name="android:background">?attr/colorSurface</item>
        <item name="android:elevation">2dp</item>
        <item name="android:padding">16dp</item>
    </style>

    <style name="CoffeeTextPrimary">
        <item name="android:textColor">?android:attr/textColorPrimary</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="CoffeeTextSecondary">
        <item name="android:textColor">?android:attr/textColorSecondary</item>
        <item name="android:textSize">14sp</item>
    </style>
</resources>